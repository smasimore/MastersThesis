All test are made to run right out of the box  on python 43 as long as the necessary libraries are installed. 
Run pip install -r requirements.txt in the hil directory to install the requirements specified in requirements.txt.
Then to run the desired test just execute the code from an IDE or the command line. No other setup is necessary. 



